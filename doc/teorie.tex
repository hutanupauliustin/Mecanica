\documentclass[12pt, a4paper]{article}
\usepackage{amsmath, amssymb, geometry, graphicx}
\usepackage[utf8]{inputenc}
\usepackage[hidelinks]{hyperref}
\usepackage{tikz}
\usetikzlibrary{patterns, arrows.meta, decorations.pathmorphing}
\geometry{a4paper, margin=2cm}
\renewcommand{\refname}{}
\renewcommand{\contentsname}{Cuprins}

\title{Motor Fizic pentru Corpuri Rigide în Plan } % \\ \large Modelarea Dinamicii și a Legăturilor Unilaterale}
\author{Huțanu Paul-Iustin \\ \small Profesor îndrumator: Craifaleanu Andrei}
\date{\today} 

\begin{document}

\maketitle
\newpage

\tableofcontents

\newpage

\section{Introducere și obiectivele lucrarii}

    % Scopul lucrarii este de a face un program care sa permita simularea miscarii unor sisteme mecanice care se studieaza la cursul de Mecanica din semsetrul I. 
\section{ Dinamica Sitemelor cu Legături}

\subsection{Cinematica sistemului de corpuri}

\begin{figure}[h!]
\centering
\begin{tikzpicture}[scale=1.5, >=stealth]

    % 1. Axele globale (Sistemul fix OXY)
    \draw[->, thick] (0,0) -- (6.5,0) node[below] {$X$};
    \draw[->, thick] (0,0) -- (0,5.5) node[left] {$Y$};
    \node[below left] at (0,0) {$O$};

    % 2. Corpul rigid 'l'
    \draw[thick, fill=blue!5] plot [smooth cycle, tension=0.8] coordinates {
        (2.3, 1.8) (4.1, 1.7) (4.5, 3.3) (3.3, 4.3) (2.5, 3.5)
    };
    \node[blue!80!black, font=\large] at (1.5, 2.0) {$l$}; % Numele corpului
        
    % Eticheta corpului
    \node[blue!80!black, font=\large] at (5.2, 2.5) {$l$};

    % 3. Centrul de masa (C_l)
    \coordinate (Cl) at (3.5, 2.6);
    \filldraw (Cl) circle (1.5pt) node[below right=2pt, inner sep=1pt] {$C_l$};

    % 4. Punctul oarecare 'j' de pe corp (distantat mai mult pt claritate)
    \coordinate (J) at (3.4, 4.0);
    \filldraw (J) circle (1.5pt) node[above right=1pt] {$j$};

    % 5. Sistemul de referinta local (atasat de C_l)
    % Linia orizontala punctata pentru referinta unghiului
    \draw[dashed, gray!80!black] (Cl) -- ++(2.2, 0);

    % Axele locale x_l si y_l rotite cu 30 de grade
    \draw[->,  very thick, blue!80!black] (Cl) -- ++(30:1.0) node[below right] {$\bar{i}_l$};
    \draw[->,  very thick, blue!80!black] (Cl) -- ++(120:1.0) node[right] {$\bar{j}_l$};

    \draw[->, dashed, blue!80!black] (Cl) -- ++(30:5.0) node[below right] {$x_l$};
    \draw[->, dashed, blue!80!black] (Cl) -- ++(120:5.0) node[right] {$y_l$};

    % Arcul pentru unghiul phi_l
    \draw[->, thick] (Cl) ++(1.4, 0) arc (0:30:1.4) node[midway, right=2pt] {$\varphi_l$};

    % 6. Vectorii de pozitie
    % Vectorul r_l (O -> C_l)
    \draw[->, very thick, red!80!black] (0,0) -- (Cl) 
        node[midway,below right=6pt, inner sep=2pt] {$\bar{r}_l$};
    
    % Vectorul r_j (O -> j)
    \draw[->, very thick, green!60!black] (0,0) -- (J) 
        node[midway,below right=4pt, inner sep=2pt] {$\bar{r}_j$};
    
    % Vectorul relativ r_jl (C_l -> j)
    \draw[->, very thick, purple] (Cl) -- (J) 
        node[midway,below right=4pt , inner sep=2pt] {$\bar{r}_{jl}$};

\end{tikzpicture}
\caption{Poziția unui punct $j$ aflat pe un corp rigid $l$ în mișcare plan-paralelă, raportat la sistemul global $OXY$ și sistemul local $C_l x_l y_l$.}
\label{fig:cinematica_corp}
\end{figure}

Considerăm un sistem de $n$ corpuri, fiecare fiind compus dintr-un număr arbitrar $N$ de puncte aflate în mișcare plan-paralelă.

Fiecare corp $l$ are un sistem de coordonate propriu $x_l C_l y_l$, aflat în centrul său de masă. Sistemul este considerat la un unghi $\varphi_l$ ce corespunde mișcării corpului. Versorii sistemului de coordonate local, $\bar{i}_l$ și $\bar{j}_l$, sunt neconstanți în raport cu sistemul fix.

Vectorul de poziție al unui punct $j$ de pe corpul $l$ în raport cu centrul de masă este:
\begin{equation}
    \bar{r}_{jl} = d_{xjl}\bar{i}_l + d_{yjl}\bar{j}_l
\end{equation}

Exprimăm versorii locali în funcție de cei ficși ($\bar{i}$, $\bar{j}$):
\begin{equation}
    \begin{aligned}
        \bar{i}_l &= \cos\varphi_l\bar{i} + \sin\varphi_l\bar{j} \\
        \bar{j}_l &= -\sin\varphi_l\bar{i} + \cos\varphi_l\bar{j}
    \end{aligned}
\end{equation}

Înlocuind, obținem:
\begin{equation}
    \bar{r}_{jl} = (d_{xjl}\cos\varphi_l - d_{yjl}\sin\varphi_l)\bar{i} + (d_{xjl}\sin\varphi_l + d_{yjl}\cos\varphi_l)\bar{j}
\end{equation}

Vectorul de poziție absolut al punctului $j$ este suma dintre vectorul de poziție al centrului de masă și vectorul relativ:
\begin{equation}
    \bar{r}_j = \bar{r}_l + \bar{r}_{jl} = 
    \begin{bmatrix}
        x_l + d_{xjl}\cos\varphi_l - d_{yjl}\sin\varphi_l \\
        y_l + d_{xjl}\sin\varphi_l + d_{yjl}\cos\varphi_l
    \end{bmatrix}
\end{equation}

Derivând în raport cu timpul, obținem viteza punctului $j$:
\begin{equation}
    \bar{v}_j = \dot{\bar{r}}_j = 
    \begin{bmatrix}
        \dot{x}_l + \dot{\varphi}_l(-d_{xjl}\sin\varphi_l - d_{yjl}\cos\varphi_l) \\
        \dot{y}_l + \dot{\varphi}_l(d_{xjl}\cos\varphi_l - d_{yjl}\sin\varphi_l)
    \end{bmatrix}
    = 
    \begin{bmatrix}
        v_{jx} \\
        v_{jy}
    \end{bmatrix}
\end{equation}

Calculăm pătratul modulului vitezei:
\begin{equation}
    \begin{gathered}
        v_j^2 = \langle\bar{v}_j, \bar{v}_j\rangle = v_{jx}^2 + v_{jy}^2 \\
        \begin{aligned}
            v_j^2 &= \dot{x}_l^2 + \dot{y}_l^2 + \dot{\varphi}_l^2(d_{xjl}^2\sin^2\varphi_l + d_{xjl}^2\cos^2\varphi_l + d_{yjl}^2\cos^2\varphi_l + d_{yjl}^2\sin^2\varphi_l) \\
            &\quad + 2\dot{\varphi}_l[\dot{x}_l(-d_{xjl}\sin\varphi_l - d_{yjl}\cos\varphi_l) + \dot{y}_l(d_{xjl}\cos\varphi_l - d_{yjl}\sin\varphi_l)]
        \end{aligned}
    \end{gathered}
\end{equation}

Deoarece $\sin^2\varphi_l + \cos^2\varphi_l = 1$, expresia se simplifică:
\begin{equation}
    v_j^2 = \dot{x}_l^2 + \dot{y}_l^2 + \dot{\varphi}_l^2(d_{xjl}^2 + d_{yjl}^2) + 2\dot{\varphi}_l[-\dot{x}_l(d_{xjl}\sin\varphi_l + d_{yjl}\cos\varphi_l) + \dot{y}_l(d_{xjl}\cos\varphi_l - d_{yjl}\sin\varphi_l)]
\end{equation}

\subsection{Energia cinetică a sistemului}

Energia cinetică a corpului $l$ este suma energiilor cinetice ale punctelor sale:
\begin{equation}
    \begin{gathered}
        E_l = \sum_{j=1}^{N} E_j = \frac{1}{2}\sum_{j=1}^{N} m_j v_j^2 \\
             E_l = \frac{1}{2} \left[ \dot{x}_l^2\sum_{j=1}^{N} m_j + \dot{y}_l^2\sum_{j=1}^{N} m_j + \dot{\varphi}_l^2\sum_{j=1}^{N} m_j(d_{xjl}^2 + d_{yjl}^2) \right] \\
             \quad + \dot{\varphi}_l \left[ (-\dot{x}_l\sin\varphi_l - \dot{y}_l\cos\varphi_l) \sum_{j=1}^{N} m_j d_{xjl} + (\dot{x}_l\cos\varphi_l - \dot{y}_l\sin\varphi_l) \sum_{j=1}^{N} m_j d_{yjl} \right]
    \end{gathered}
\end{equation}

Știind că originea sistemului local se află în centrul de masă al corpului, momentele statice sunt nule ($S_x = S_y = 0$):
\begin{equation}
    \sum_{j=1}^{N} m_j = M_l, \quad \sum_{j=1}^{N} m_j d_{xjl} = S_{yl} = 0, \quad \sum_{j=1}^{N} m_j d_{yjl} = S_{xl} = 0
\end{equation}
iar momentul de inerție față de axa $z$ este $J_{zl} = \sum_{j=1}^{N} m_j(d_{xjl}^2 + d_{yjl}^2)$.

Astfel, termenii care conțin $d_{xjl}$ și $d_{yjl}$ la puterea întâi se anulează, deci energia cinetică a corpului $l$ capătă forma:
\begin{equation}
    E_l = \frac{1}{2} M_l \dot{x}_l^2 + \frac{1}{2} M_l \dot{y}_l^2 + \frac{1}{2} J_{zl} \dot{\varphi}_l^2
\end{equation}

Pentru întregul sistem, energia este suma energiilor corpurilor componente:
\begin{equation}
    E = \sum_{l=1}^{n} E_l = \frac{1}{2}\sum_{l=1}^{n} (M_l\dot{x}_l^2 + M_l\dot{y}_l^2 + J_{zl}\dot{\varphi}_l^2)
\end{equation}

Considerăm vectorul coordonatelor generalizate pentru un sistem cu $n$ corpuri, având o singură matrice coloană:
$q = [x_1, y_1, \varphi_1, x_2, y_2, \varphi_2, \dots, x_n, y_n, \varphi_n]^T$. 

Energia cinetică se poate scrie ca o formă pătratică:
\begin{equation}
    E = \frac{1}{2}\dot{q}^T A \dot{q}
\end{equation}
unde $A$ este matricea maselor, o matrice diagonală de forma:
\begin{equation}
    A = \begin{bmatrix}
        M_1 & 0 & 0 & \dots & 0 & 0 & 0 \\
        0 & M_1 & 0 & \dots & 0 & 0 & 0 \\
        0 & 0 & J_{z1} & \dots & 0 & 0 & 0 \\
        \vdots & \vdots & \vdots & \ddots & \vdots & \vdots & \vdots \\
        0 & 0 & 0 & \dots & M_n & 0 & 0 \\
        0 & 0 & 0 & \dots & 0 & M_n & 0 \\
        0 & 0 & 0 & \dots & 0 & 0 & J_{zn}
    \end{bmatrix}
\end{equation}

\subsection{Ecuațiile lui Lagrange pentru sisteme cu legături}

Fie ecuațiile lui Lagrange în cazul sistemelor cu variabile dependente, introducând un sistem de constrângeri (legături cinematice olonome):
\begin{equation}
    \begin{cases}
        f_1(q_1, \dots, q_{3n}, t) = 0 \\
        \vdots \\
        f_p(q_1, \dots, q_{3n}, t) = 0
    \end{cases}
\end{equation}

Notăm $F(q,t) = \big( f_1(q,t), f_2(q,t), \dots, f_p(q,t) \big)^T$. 
Putem rescrie legăturile sub forma compactă $F(q,t) = \mathbf{0}$.

Ecuațiile lui Lagrange de speța întâi iau forma:
\begin{equation}
    \frac{d}{dt}\left(\frac{\partial E}{\partial \dot{q}_k}\right) - \frac{\partial E}{\partial q_k} = Q_k + \sum_{j=1}^{p} \lambda_j \frac{\partial f_j}{\partial q_k}
\end{equation}

Calculăm Jacobianul funcțiilor de legătură $F$:
\begin{equation}
    J_F = \begin{bmatrix}
        \frac{\partial f_1}{\partial q_1} & \frac{\partial f_1}{\partial q_2} & \dots & \frac{\partial f_1}{\partial q_{3n}} \\
        \vdots & \vdots & \ddots & \vdots \\
        \frac{\partial f_p}{\partial q_1} & \frac{\partial f_p}{\partial q_2} & \dots & \frac{\partial f_p}{\partial q_{3n}}
    \end{bmatrix}
\end{equation}

Fie vectorul coloană al multiplicatorilor lui Lagrange $\Lambda = [\lambda_1, \dots, \lambda_p]^T$. Observăm că atunci când calculăm produsul $J_F^T \Lambda$ obținem exact termenii forțelor de legătură din ecuațiile lui Lagrange:
\begin{equation}
    J_F^T \Lambda = 
    \begin{bmatrix}
        \frac{\partial f_1}{\partial q_1} & \dots & \frac{\partial f_p}{\partial q_1} \\
        \vdots & \ddots & \vdots \\
        \frac{\partial f_1}{\partial q_{3n}} & \dots & \frac{\partial f_p}{\partial q_{3n}}
    \end{bmatrix}
    \begin{bmatrix}
        \lambda_1 \\ \vdots \\ \lambda_p
    \end{bmatrix}
    =
    \begin{bmatrix}
        \sum_{j=1}^p \lambda_j \frac{\partial f_j}{\partial q_1} \\
        \vdots \\
        \sum_{j=1}^p \lambda_j \frac{\partial f_j}{\partial q_{3n}}
    \end{bmatrix}
\end{equation}

Când scriem ecuațiile lui Lagrange desfășurat, forma matriceală devine:
\begin{equation}
    \frac{d}{dt}\left(\frac{\partial E}{\partial \dot{q}}\right) - \frac{\partial E}{\partial q} = Q + J_F^T \Lambda
\end{equation}

\subsubsection{Calculul derivatelor energiei}

Calculăm derivata parțială a energiei în raport cu componenta $\dot{q}_k$:
\begin{equation}
    \frac{\partial E}{\partial \dot{q}_k} = \frac{\partial}{\partial \dot{q}_k} \left( \frac{1}{2} \dot{q}^T A \dot{q} \right) = \frac{1}{2} \left[ \frac{\partial \dot{q}^T}{\partial \dot{q}_k} A \dot{q} + \dot{q}^T \frac{\partial A}{\partial \dot{q}_k} \dot{q} + \dot{q}^T A \frac{\partial \dot{q}}{\partial \dot{q}_k} \right]
\end{equation}

Deoarece matricea $A$ este constantă, derivata ei este zero. Derivata vectorului $\dot{q}$ în raport cu elementul $\dot{q}_k$ este un vector coloană $e_k$ care are $1$ pe poziția $k$ și $0$ în rest:
\begin{equation}
    \frac{\partial E}{\partial \dot{q}_k} = \frac{1}{2} \bigg[ \begin{bmatrix} 0 & 0 & \dots & 1 & \dots & 0 & 0\end{bmatrix} A \dot{q} + 0 + \dot{q}^T A \begin{bmatrix} 0 \\ \vdots \\ 1 \\ \vdots \\ 0 \end{bmatrix} \bigg] = \frac{1}{2} [ e_k^T A \dot{q} + \dot{q}^T A e_k ]
\end{equation}

Dacă notăm $e_k^T A \dot{q}$ cu $B$, atunci $\dot{q}^T A e_k = B^T$. Deoarece dimensiunile matricelor $e_k^T$, $A$ și $\dot{q}$ sunt $1 \times 3n$, $3n \times 3n$ și respectiv $3n \times 1$, rezultă că $B$ este o matrice $1 \times 1$, deci $B$ este un scalar. Pentru un scalar, $B = B^T$.
\begin{equation}
    \frac{\partial E}{\partial \dot{q}_k} = \frac{1}{2} (B + B^T) = B = e_k^T A \dot{q}
\end{equation}

Cum matricea $A$ este diagonală, produsul extrage pur și simplu elementul de pe diagonala principală corespunzător poziției $k$:
\begin{equation}
    \frac{\partial E}{\partial \dot{q}_k} = a_{k,k} \dot{q}_k
\end{equation}

Derivând această expresie în raport cu timpul, obținem:
\begin{equation}
    \frac{d}{dt} \left( \frac{\partial E}{\partial \dot{q}_k} \right) = a_{k,k} \ddot{q}_k
\end{equation}

Rezultă asamblând pentru întregul vector coloană:
\begin{equation}
    \frac{d}{dt}\left(\frac{\partial E}{\partial \dot{q}}\right) = \frac{d}{dt}
    \begin{bmatrix}
        \frac{\partial E}{\partial \dot{q}_1} \\ \vdots \\ \frac{\partial E}{\partial \dot{q}_{3n}}
    \end{bmatrix}
    = A\ddot{q}
\end{equation}

Calculăm apoi derivata energiei în raport cu coordonata $q_k$:
\begin{equation}
    \frac{\partial E}{\partial q_k} = \frac{1}{2} \left[ \frac{\partial \dot{q}^T}{\partial q_k} A \dot{q} + \dot{q}^T \frac{\partial A}{\partial q_k} \dot{q} + \dot{q}^T A \frac{\partial \dot{q}}{\partial q_k} \right] = 0 + 0 + 0 = 0
\end{equation}
Acest lucru se întâmplă deoarece vitezele $\dot{q}$ și matricea constantă $A$ nu depind de $q_k$.

Notăm vectorul forțelor generalizate cu $Q = \begin{bmatrix} Q_1 \\ \vdots \\ Q_K \end{bmatrix}$. 

Înlocuind toate acestea în ecuațiile lui Lagrange de speța întâi, ajungem la forma finală:
\begin{equation}
    A\ddot{q} = Q + J_F^T \Lambda
\end{equation}

la care atașăm sistemul de constrângeri $F(q,t) = 0$.

\subsubsection{Rezolvarea sistemului de ecuații}

Sistemul format din ecuațiile de mișcare și ecuațiile de constrângere este:
\begin{equation}
    \begin{cases}
        F(q) = 0 \\
        A\ddot{q} = Q + J_F^T \Lambda
    \end{cases}
\end{equation}
Acest sistem nu este suficient pentru a calcula direct componentele lui $q$ și multiplicatorii $\Lambda$, deoarece din prima ecuație putem obține coordonatele $q$, dar în a doua ecuație avem nevoie de accelerațiile $\ddot{q}$.

Pentru a rezolva această problemă, derivăm prima ecuație (condiția de legătură) de două ori în raport cu timpul:
\begin{equation}
    \frac{d}{dt}\left[ \frac{d}{dt}F(q) \right] = 0 \implies \frac{d}{dt}(J_F \dot{q}) = 0
\end{equation}
Aplicând regula produsului, ajungem la ecuația constrângerilor la nivel de accelerații:
\begin{equation}
    J_F \ddot{q} + \dot{J}_F \dot{q} = 0
\end{equation}

Din ecuația de mișcare exprimăm accelerațiile $\ddot{q}$:
\begin{equation}
    A\ddot{q} = Q + J_F^T \Lambda \implies \ddot{q} = A^{-1}Q + A^{-1}J_F^T \Lambda
\end{equation}

Înlocuim expresia lui $\ddot{q}$ în ecuația derivată a constrângerilor:
\begin{equation}
    J_F (A^{-1}Q + A^{-1}J_F^T \Lambda) + \dot{J}_F \dot{q} = 0
\end{equation}
Desfăcând parantezele și separând termenul care îl conține pe $\Lambda$, obținem forma finală a sistemului liniar din care se pot calcula multiplicatorii lui Lagrange:
\begin{equation}
    (J_F A^{-1} J_F^T)\Lambda = -\dot{J}_F \dot{q} - J_F A^{-1} Q
\end{equation}
Observăm ca matricea $\dot{J}$ nu apare de sine stătătoare, și este înmulțită cu $\dot{q}$. Rezultatul înmulțirii este un vector. În cod, nu vom salva valoarea matricei $\dot{J}$, și vom salva direct produsul înmulțirii pentru a economisi memorie.

\subsection{Introducerea constrângerilor}

Legăturile le vom realiza între puncte specifice aflate pe corpuri diferite. Pentru a formula ecuațiile de constrângere, trebuie să exprimăm coordonatele acestor puncte din sistemele locale în sistemul de referință global.

Fie un punct $P$ aflat pe un corp arbitrar $l$. Dacă notăm coordonatele locale ale punctului $P$ față de centrul de masă al corpului cu $l_{xl}$ și $l_{yl}$, coordonatele sale globale vor fi date de transformarea:
\begin{equation}
    \begin{aligned}
        x_P &= x_l + l_{xl}\cos\varphi_l - l_{yl}\sin\varphi_l \\
        y_P &= y_l + l_{xl}\sin\varphi_l + l_{yl}\cos\varphi_l
    \end{aligned}
\end{equation}
unde $(x_l, y_l, \varphi_l)$ definesc poziția centrului de masă și orientarea corpului $l$ în planul global.

Când două corpuri, $A$ și $B$, sunt legate, trebuie să adăugăm componente noi funcției F. Astfel se va schimba și Jacobianul $J_F$.
\begin{equation}
    F = \big( f_1(q), \dots, f_p(q), f_{p+1}(q), \dots \big)^T
\end{equation}

\subsubsection{Articulația}
Introducerea unei articulații între corpurile $A$ și $B$ forțează punctele lor de legătură să coincidă ($x_{PA} = x_{PB}$ și $y_{PA} = y_{PB}$). Aceasta adaugă două constrângeri la sistem și doi multiplicatori Lagrange:
\begin{equation}
    \begin{aligned}
        f_{p+1} &= x_A + l_{xA}\cos\varphi_A - l_{yA}\sin\varphi_A - (x_B + l_{xB}\cos\varphi_B - l_{yB}\sin\varphi_B) = 0 \\
        f_{p+2} &= y_A + l_{xA}\sin\varphi_A + l_{yA}\cos\varphi_A - (y_B + l_{xB}\sin\varphi_B + l_{yB}\cos\varphi_B) = 0
    \end{aligned}
\end{equation}

Calculăm derivatele parțiale ale funcțiilor $f_{p+1}$ și $f_{p+2}$ pentru a le adăuga în Jacobian:
\begin{equation}
    \begin{aligned}
        \frac{\partial f_{p+1}}{\partial x_A} &= 1, \quad \frac{\partial f_{p+1}}{\partial x_B} = -1 \\
        \frac{\partial f_{p+1}}{\partial \varphi_A} &= -l_{xA}\sin\varphi_A - l_{yA}\cos\varphi_A \\
        \frac{\partial f_{p+1}}{\partial \varphi_B} &= l_{xB}\sin\varphi_B + l_{yB}\cos\varphi_B
    \end{aligned}
\end{equation}
\begin{equation}
    \begin{aligned}
        \frac{\partial f_{p+2}}{\partial y_A} &= 1, \quad \frac{\partial f_{p+2}}{\partial y_B} = -1 \\
        \frac{\partial f_{p+2}}{\partial \varphi_A} &= l_{xA}\cos\varphi_A - l_{yA}\sin\varphi_A \\
        \frac{\partial f_{p+2}}{\partial \varphi_B} &= -l_{xB}\cos\varphi_B + l_{yB}\sin\varphi_B
    \end{aligned}
\end{equation}
Toate celelalte derivate parțiale în raport cu coordonatele corpurilor care nu sunt implicate în articulație vor fi $0$.

La nivel matriceal, introducerea articulației adaugă următorii vectori coloană la matricea $J_F^T$:
\begin{equation}
    \begin{bmatrix}
        0 \\ \vdots \\ 1 \\ 0 \\ -l_{xA}\sin\varphi_A - l_{yA}\cos\varphi_A \\ \vdots \\ -1 \\ 0 \\ l_{xB}\sin\varphi_B + l_{yB}\cos\varphi_B \\ \vdots \\ 0
    \end{bmatrix}
    \quad \text{și} \quad
    \begin{bmatrix}
        0 \\ \vdots \\ 0 \\ 1 \\ l_{xA}\cos\varphi_A - l_{yA}\sin\varphi_A \\ \vdots \\ 0 \\ -1 \\ -l_{xB}\cos\varphi_B + l_{yB}\sin\varphi_B \\ \vdots \\ 0
    \end{bmatrix}
\end{equation}

\paragraph{Calculul matricelor pentru Articulație}

Pentru o articulație, am definit deja ecuațiile de legătură $f_{p+1}$ și $f_{p+2}$:
\begin{equation}
    \begin{aligned}
        f_{p+1} &= x_A + l_{xA}\cos\varphi_A - l_{yA}\sin\varphi_A - (x_B + l_{xB}\cos\varphi_B - l_{yB}\sin\varphi_B) = 0 \\
        f_{p+2} &= y_A + l_{xA}\sin\varphi_A + l_{yA}\cos\varphi_A - (y_B + l_{xB}\sin\varphi_B + l_{yB}\cos\varphi_B) = 0
    \end{aligned}
\end{equation}

Prin derivarea matricelor în raport cu timpul, articulația introduce în matricea $\dot{J}_F$ următoarele două linii (corespunzătoare coordonatelor corpurilor $A$ și $B$):
\begin{equation}
    \begin{bmatrix}
        0 & 0 & -l_{xA}\dot{\varphi}_A\cos\varphi_A + l_{yA}\dot{\varphi}_A\sin\varphi_A & \dots & 0 & 0 & l_{xB}\dot{\varphi}_B\cos\varphi_B - l_{yB}\dot{\varphi}_B\sin\varphi_B \\
        0 & 0 & -l_{xA}\dot{\varphi}_A\sin\varphi_A - l_{yA}\dot{\varphi}_A\cos\varphi_A & \dots & 0 & 0 & l_{xB}\dot{\varphi}_B\sin\varphi_B + l_{yB}\dot{\varphi}_B\cos\varphi_B
    \end{bmatrix}
\end{equation}

Acești termeni intervin în calculul vectorului $\dot{J}_F \dot{q}$. Înmulțind liniile de mai sus cu vectorul coloană al vitezelor $\dot{q} = [\dots, \dot{x}_A, \dot{y}_A, \dot{\varphi}_A, \dots, \dot{x}_B, \dot{y}_B, \dot{\varphi}_B, \dots]^T$, obținem componentele adăugate de articulație la vectorul $\dot{J}_F \dot{q}$:
\begin{equation}
    \begin{bmatrix}
        -l_{xA}\dot{\varphi}_A^2\cos\varphi_A + l_{yA}\dot{\varphi}_A^2\sin\varphi_A + l_{xB}\dot{\varphi}_B^2\cos\varphi_B - l_{yB}\dot{\varphi}_B^2\sin\varphi_B \\
        -l_{xA}\dot{\varphi}_A^2\sin\varphi_A - l_{yA}\dot{\varphi}_A^2\cos\varphi_A + l_{xB}\dot{\varphi}_B^2\sin\varphi_B + l_{yB}\dot{\varphi}_B^2\cos\varphi_B
    \end{bmatrix}
\end{equation}

\subsubsection{Încastrarea}
Adăugarea unei încastrări presupune prezența constrângerilor de la articulație ($f_{p+1}$, $f_{p+2}$), la care se adaugă o componentă suplimentară ce blochează rotația relativă a celor două corpuri:
\begin{equation}
    f_{p+3} = \varphi_A - \varphi_B - \varphi_0 = 0
\end{equation}
unde $\varphi_0$ este unghiul relativ inițial la momentul încastrării.

Derivatele parțiale ale noii funcții $f_{p+3}$ vor fi simplu:
\begin{equation}
    \frac{\partial f_{p+3}}{\partial \varphi_A} = 1, \quad \frac{\partial f_{p+3}}{\partial \varphi_B} = -1
\end{equation}

Matriceal, pe lângă cele două coloane adăugate de articulație, încastrarea mai adaugă un vector coloană în matricea $J_F^T$:
\begin{equation}
    \begin{bmatrix}
        0 \\ \vdots \\ 1 \\ 0 \\ -l_{xA}\sin\varphi_A - l_{yA}\cos\varphi_A \\ \vdots \\ -1 \\ 0 \\ l_{xB}\sin\varphi_B + l_{yB}\cos\varphi_B \\ \vdots \\ 0
    \end{bmatrix}
    \quad \text{,} \quad
    \begin{bmatrix}
        0 \\ \vdots \\ 0 \\ 1 \\ l_{xA}\cos\varphi_A - l_{yA}\sin\varphi_A \\ \vdots \\ 0 \\ -1 \\ -l_{xB}\cos\varphi_B + l_{yB}\sin\varphi_B \\ \vdots \\ 0
    \end{bmatrix}
    \quad \text{și} \quad
    \begin{bmatrix}
        0 \\ \vdots \\ 0 \\ 0 \\ 1 \\ \vdots \\ 0 \\ 0 \\ -1 \\ \vdots \\ 0
    \end{bmatrix}
\end{equation}

\paragraph{Calculul vectorului $\dot{J}_F \dot{q}$ pentru Încastrare}

Pentru o încastrare, sistemul de ecuații de legătură conține ecuațiile articulației ($f_{p+1}$ și $f_{p+2}$) la care se adaugă o ecuație suplimentară pentru blocarea rotației relative:
\begin{equation}
    f_{p+3} = \varphi_A - \varphi_B - \varphi_0 = 0
\end{equation}

Liniile corespunzătoare funcțiilor $f_{p+1}$ și $f_{p+2}$ în matricea $\dot{J}_F$ și, implicit, în vectorul $\dot{J}_F \dot{q}$, rămân absolut identice cu cele calculate anterior pentru articulație. 

Pentru funcția $f_{p+3}$, derivatele parțiale care intră în Jacobian sunt constante:
\begin{equation}
    \frac{\partial f_{p+3}}{\partial \varphi_A} = 1, \quad \frac{\partial f_{p+3}}{\partial \varphi_B} = -1
\end{equation}
Deoarece aceste derivate sunt valori constante (nu depind nici de timp, nici de coordonate), derivata lor în raport cu timpul pentru a forma matricea $\dot{J}_F$ este nulă. Astfel, linia adăugată de $f_{p+3}$ în matricea $\dot{J}_F$ este formată exclusiv din zerouri:
\begin{equation}
    \begin{bmatrix}
        0 & 0 & 0 & \dots & 0 & 0 & 0
    \end{bmatrix}
\end{equation}

Prin urmare, atunci când calculăm produsul $\dot{J}_F \dot{q}$, componenta corespunzătoare ecuației $f_{p+3}$ va fi $0$. Forma finală a componentelor introduse de o încastrare în vectorul $\dot{J}_F \dot{q}$ este un vector coloană de dimensiune $3 \times 1$:
\begin{equation}
    \begin{bmatrix}
        -l_{xA}\dot{\varphi}_A^2\cos\varphi_A + l_{yA}\dot{\varphi}_A^2\sin\varphi_A + l_{xB}\dot{\varphi}_B^2\cos\varphi_B - l_{yB}\dot{\varphi}_B^2\sin\varphi_B \\
        -l_{xA}\dot{\varphi}_A^2\sin\varphi_A - l_{yA}\dot{\varphi}_A^2\cos\varphi_A + l_{xB}\dot{\varphi}_B^2\sin\varphi_B + l_{yB}\dot{\varphi}_B^2\cos\varphi_B \\
        0
    \end{bmatrix}
\end{equation}
\subsection{Fortele dintre corpuri}
\subsubsection{Forte elastice}

\begin{figure}[h!]
\centering
\begin{tikzpicture}[scale=1.5, >=stealth]

    % 1. Axele globale (Sistemul fix OXY)
    \draw[->, thick] (0,0) -- (7.5,0) node[below] {$X$};
    \draw[->, thick] (0,0) -- (0,5.0) node[left] {$Y$};
    \node[below left] at (0,0) {$O$};

    % 2. Corpul rigid 'A'
    \draw[thick, fill=blue!5] plot [smooth cycle, tension=0.8] coordinates {
        (1.0, 1.5) (2.8, 1.2) (3.2, 2.8) (2.0, 3.8) (1.2, 3.0)
    };
    % Numele corpului mutat mai sus la stanga
    \node[blue!80!black, font=\large] at (1.2, 3.5) {$A$}; 

    \coordinate (CA) at (2.0, 2.5);
    \filldraw (CA) circle (1.5pt) node[left=3pt] {$C_A$}; 

    \coordinate (PtA) at (2.6, 3.2);
    \filldraw (PtA) circle (1.5pt) node[above=3pt] {$A$}; % Punctul de prindere

    % 3. Corpul rigid 'B'
    \draw[thick, fill=red!5] plot [smooth cycle, tension=0.8] coordinates {
        (4.5, 2.5) (6.5, 2.0) (7.2, 3.5) (5.8, 4.8) (4.8, 4.0)
    };
    % Numele corpului B mutat mai in dreapta
    \node[red!80!black, font=\large] at (6.6, 2.5) {$B$};

    \coordinate (CB) at (5.8, 3.4);
    \filldraw (CB) circle (1.5pt) node[right=3pt] {$C_B$}; 

    \coordinate (PtB) at (4.8, 3.0);
    \filldraw (PtB) circle (1.5pt) node[above right=2pt] {$B$};

    % 4. Vectorii globali de pozitie
    \draw[->, very thick, green!60!black] (0,0) -- (PtA) 
        node[pos=0.6, above left=1pt] {$\bar{r}_1$}; 
    \draw[->, very thick, green!60!black] (0,0) -- (PtB) 
        node[pos=0.6, below right=1pt] {$\bar{r}_2$};

    % 5. Vectorii locali de pozitie
    \draw[->, very thick, purple] (CA) -- (PtA) 
        node[midway, above left=1pt] {$\bar{\rho}_A$}; 
    \draw[->, very thick, purple] (CB) -- (PtB) 
        node[midway, below right=1pt] {$\bar{\rho}_B$}; 

    % 6. Arcul elastic (foloseste decoratia zigzag)
    \draw[decorate, decoration={zigzag, segment length=3mm, amplitude=1.5mm}, thick, orange!90!black] (PtA) -- (PtB);

    % 7. Versorul directiei si forta aplicata (MUTATE DE PE ARC)
    % Desenate paralel, un pic mai sus si mai jos
    \draw[->, very thick, orange!80!black] ([yshift=3mm]PtA) -- ++(1.4, -0.127) 
        node[above] {$\bar{F}_e$};
    \draw[->, very thick, blue] ([yshift=-3.5mm]PtA) -- ++(0.8, -0.072) 
        node[below] {$\overline{u}$};

\end{tikzpicture}
\caption{Reprezentarea unui arc elastic, interacționând între punctele de prindere $A$ și $B$ a două corpuri rigide distincte.}
\label{fig:arc_elastic}
\end{figure}

Pentru a defini un arc între două corpuri, considerăm punctele de prindere $A$ și $B$, definite prin vectorii de poziție locali $\bar{\rho}_A = [l_{xA}, l_{yA}]^T$ și $\bar{\rho}_B = [l_{xB}, l_{yB}]^T$. 
Pozițiile globale ale acestor puncte, $\bar{r}_1$ și $\bar{r}_2$, se calculează folosind matricele de rotație ale corpurilor.

Lungimea curentă a arcului $l$ este norma diferenței celor doi vectori de poziție:
\begin{equation}
    l = ||\bar{r}_2 - \bar{r}_1|| = \sqrt{(X_2 - X_1)^2 + (Y_2 - Y_1)^2}
\end{equation}

Versorul direcției arcului (care arată de la corpul A spre corpul B) este:
\begin{equation}
    \overline{u} = \frac{\bar{r}_2 - \bar{r}_1}{||\bar{r}_2 - \bar{r}_1||}
\end{equation}

Conform legii lui Hooke, forța elastică generată de arc este proporțională cu deformația sa $\Delta x = l - l_0$, unde $l_0$ este lungimea de repaus. Vectorul forței aplicate corpului A va fi:
\begin{equation}
    \bar{F}_{e} = - k (l - l_0) \overline{u}
\end{equation}
\textit{(Notă: Corpul B va acționa cu aceeași forță, dar de sens opus: $-\bar{F}_{e}$, conform principiului acțiunii și reacțiunii).}

Trebuie să considerăm și momentul generat de forța elastică:
\begin{equation}
    \bar{M}_{CA}(\bar{F}_e) = \bar{\rho}_A \times \bar{F}_e = 
    \begin{vmatrix} 
    \bar{i} & \bar{j} & \bar{k} \\ 
    l_{xA} & l_{yA} & 0 \\ 
    F_{ex} & F_{ey} & 0 
    \end{vmatrix} 
    = (l_{xA} F_{ey} - l_{yA} F_{ex})\bar{k}
\end{equation}

Pentru corpul B, forța aplicată este reacțiunea ($-\bar{F}_e$):
\begin{equation}
    \bar{M}_{CB}(-\bar{F}_e) = \bar{\rho}_B \times (-\bar{F}_e) = 
    \begin{vmatrix} 
    \bar{i} & \bar{j} & \bar{k} \\ 
    l_{xB} & l_{yB} & 0 \\ 
    -F_{ex} & -F_{ey} & 0 
    \end{vmatrix} 
    = (-l_{xB} F_{ey} + l_{yB} F_{ex})\bar{k}
\end{equation}

Datorita erorilor aparute din cauza aritmeticii cu virgula mobila, nu putem folosi arcuri ideale din cauza riscului de crestere a energiei. Suntem obligati sa lucram cu arcuri amortizate. Diferenta apare cu un termen nou de amortizare, forta devenind:
\begin{equation} 
   \bar{F}_{e} = - ( k (l - l_0)  + d  \langle \overline{v_B} - \overline{v_A}, \overline{u}  \rangle ) \overline{u}
\end{equation}
\newpage

\section{Detecția și Rezolvarea Coliziunilor}

\subsection{Rolul detecției}
Pentru a putea rezolva legăturile unilaterale, trebuie să vedem lucrurile altfel. Deoarece ele se prezintă sub formă de inegalități, față de cele bilaterale care sunt egalități, nu le putem introduce direct în constrângeri (poziții, viteze, accelerații) cu scopul de a rezolva sistemul matricial.

O idee simplă este de a le introduce ca linie în matrice, doar dacă este cazul. În cazul unui plan înclinat, forța normală la plan apare doar dacă corpurile se ating ($\vec{N} \ge \vec{0}$). Aceasta poate fi considerată ca o constrângere de geometrie. Totuși, acest lucru vine cu alte probleme de stabilitate.

Soluția pe care o folosim este să implementăm constrângerile geometrice prin adăugarea de impulsuri. Indiferent de metodă, forța normală poate fi interpretată ca o condiție, în loc de o inegalitate: există forță doar dacă corpurile se ating. Din acest motiv, avem nevoie de un algoritm robust care verifică coliziunile între corpuri.

\subsection{Faza de filtrare (intersecția pe scară largă)}
Ideea de bază este că într-un sistem sunt mult mai multe corpuri care nu se intersectează, decât corpuri care se ating efectiv. Cel mai simplu mod de a detecta coliziunile este de a verifica toate corpurile două câte două, dar acest lucru atrage o complexitate computațională de $\mathcal{O}(n^2)$.

Din cauza geometriilor, o detecție precisă între două corpuri trebuie să ia în considerare rotațiile lor și să verifice extremitățile formelor. Pentru a eficientiza simularea, separăm verificarea în două etape: o etapă foarte rapidă, dar care nu garantează rezultatul (Broad-Phase), și o etapă care verifică exact impactul, dar folosește o matematică mai complexă (Narrow-Phase / SAT).

\begin{figure}[h!]
\centering
\begin{tikzpicture}[scale=1.5, >=stealth]

    % 1. Axele globale (sistemul fix)
    \draw[dashed, gray!60] (-3,0) -- (3,0);
    \draw[dashed, gray!60] (0,-2.5) -- (0,2.5);
    
    % Centrul C
    \filldraw (0,0) circle (1pt) node[below=2pt] {$C$};

    % 2. Corpul rotit (P_A)
    \begin{scope}[rotate=30]
        % Dreptunghiul (Bara)
        \draw[thick, fill=blue!15] (-2,-1) rectangle (2,1);
        \node[blue!80!black] at (-0.5, 0.5) {$P_A$};
        
        % Axele locale
        \draw[->, thick, blue] (0,0) -- (2.5,0) node[right] {$\vec{i}_l$};
        \draw[->, thick, blue] (0,0) -- (0,1.5) node[above] {$\vec{j}_l$};
    \end{scope}
    
    % Unghiul phi
    \draw[->, thick] (1.5,0) arc (0:30:1.5) node[midway, right=1pt] {$\varphi$};

    % 3. Cutia de incadrare B_A (Bounding Box)
    \draw[thick, red, dashed] (-2.232, -1.866) rectangle (2.232, 1.866);
    \node[red] at (-1.8, 1.5) {$B_A$};

    % 4. Cotele
    % Sageata orizontala (jos) - folosim fill=white pentru ca textul sa "taie" linia
    \draw[<->, gray!80!black] (-2.232, -2.1) -- (2.232, -2.1) 
          node[midway, fill=white, text=black, inner sep=2pt] {$2 \cdot \text{razaLatime}$};
    
    % Sageata verticala (dreapta) - text rotit la 90 de grade
    \draw[<->, gray!80!black] (2.5, -1.866) -- (2.5, 1.866) 
          node[midway, fill=white, text=black, inner sep=2pt, rotate=90] {$2 \cdot \text{razaInaltime}$};

\end{tikzpicture}
\caption{Reprezentarea unui corp rigid $P_A$ rotit, încadrat de cutia sa $B_A$ (Axis-Aligned Bounding Box).}
\label{fig:aabb}
\end{figure}
\subsection{Demonstrația matematică a fazei de filtrare}

În jurul fiecărui corp considerăm o cutie de încadrare (Bounding Box), cu laturile mereu paralele cu axele sistemului de coordonate.

Fie $P_A$, $P_B$ mulțimile punctelor care formează corpurile $A$, respectiv $B$. \\
Fie $B_A$, $B_B$ mulțimile punctelor care formează cutiile de încadrare corespunzătoare.

Prin definiție, orice corp este conținut integral în cutia sa de încadrare:
\begin{equation}
    P_A \subseteq B_A \quad \text{și} \quad P_B \subseteq B_B
\end{equation}

\paragraph{Cazul I: Cutiile nu se intersectează}
Dacă testul rapid al cutiilor returnează mulțimea vidă:
\begin{equation}
    B_A \cap B_B = \emptyset \implies P_A \cap P_B = \emptyset
\end{equation}
Deci corpurile sigur nu se intersectează, deci putem opri verificarea pentru această pereche.

\paragraph{Cazul II: Cutiile se intersectează}

\begin{figure}[h!]
\centering
\begin{tikzpicture}[scale=1.5, >=stealth]

    % 1. Zona de intersectie a aerului (Galbena)
    % Trasata sub corpuri ca sa aiba un efect de evidentiere fin (highlighter)
    \fill[yellow!40] (0.73, 0.153) rectangle (1.235, 1.752);

    % --- CORPUL A ---
    \begin{scope}[shift={(-1,0)}, rotate=25]
        \draw[thick, fill=blue!15] (-2,-1) rectangle (2,1);
        \node[blue!80!black] at (0,0) {$P_A$};
    \end{scope}
    
    % Cutia de Incadrare A (Albastru intrerupt)
    \draw[thick, blue, dashed] (-3.235, -1.752) rectangle (1.235, 1.752);
    \node[blue] at (-2.9, 1.5) {$B_A$};

    % --- CORPUL B ---
    \begin{scope}[shift={(2.2, 1.5)}, rotate=-40]
        \draw[thick, fill=red!15] (-1.5,-0.5) rectangle (1.5,0.5);
        \node[red!80!black] at (0,0) {$P_B$};
    \end{scope}
    
    % Cutia de Incadrare B (Rosu intrerupt)
    \draw[thick, red, dashed] (0.73, 0.153) rectangle (3.67, 2.847);
    \node[red] at (3.3, 2.6) {$B_B$};

    % --- ADNOTARI SI SAGEATA ---
    \draw[<-, thick, gray!80!black] (1.1, 0.8) -- (2.5, -0.5) node[right, text=black, align=left] {
        Intersecția cutiilor ($B_A \cap B_B \neq \emptyset$) \\ 
        "Aerul" se suprapune ($M_A \cap M_B \neq \emptyset$) \\
        Corpurile \textbf{nu} se ating ($P_A \cap P_B = \emptyset$)
    };

\end{tikzpicture}
\caption{Ilustrarea unui caz de "fals pozitiv" în faza de filtrare (Broad-Phase). Cutiile de încadrare se intersectează, dar formele geometrice reale trec una pe lângă cealaltă fără coliziune.}
\label{fig:broadphase_falsepositive}
\end{figure}

Dacă testul detectează o suprapunere:
\begin{equation}
    B_A \cap B_B \neq \emptyset
\end{equation}
Acest rezultat \textbf{nu} garantează faptul că $P_A \cap P_B \neq \emptyset$. Prin urmare, trebuie să mai facem o verificare. Demonstrația este următoarea:

Fie $M_A$ și $M_B$ mulțimile punctelor de "aer" (spațiul gol) din interiorul cutiilor, definite ca diferența dintre cutie și corp:
\begin{equation}
    M_A = B_A \setminus P_A \implies B_A = M_A \cup P_A
\end{equation}
\begin{equation}
    M_B = B_B \setminus P_B \implies B_B = M_B \cup P_B
\end{equation}

Dezvoltând intersecția celor două cutii, obținem:
\begin{align}
    B_A \cap B_B &= (M_A \cup P_A) \cap (M_B \cup P_B) \\
    &= (M_A \cap M_B) \cup (M_A \cap P_B) \cup (P_A \cap M_B) \cup (P_A \cap P_B) \neq \emptyset
\end{align}

Privind această ecuație, este total posibilă situația în care:
\begin{equation}
    M_A \cap M_B \neq \emptyset \quad \text{și} \quad P_A \cap P_B = \emptyset
\end{equation}
Acest scenariu reprezintă situația fizică în care doar "aerul" din cele două cutii de încadrare se suprapune, în timp ce corpurile trec unul pe lângă celălalt fără să se atingă direct.

\textbf{Concluzie:} Testul la scară largă nu este suficient pentru a verifica și confirma coliziunea efectivă. Din acest motiv, motorul fizic necesită implementarea unui algoritm de detecție detaliată care să verifice strict intersecția formelor reale. Deși, la prima vedere pare ca complicăm lucrurile și adăugam operații în plus, timpul pierdut prin necesitatea de a face două verificari pentru corpurile care sunt apropariate unele de altele, este cu mult mai puțin decât timpul pe care l-am fi pierdut dacă am fi facut verificari complexe între toate corpurile. 

\subsection{Faza de detecție exactă (Narrow-Phase)}

\subsubsection{Algoritmul de detectare}

\subsubsection{Algoritmul de clipping }

\begin{figure}[h]
\centering
\begin{tikzpicture}[scale=1.5]
    % Desenam axa de referinta si segmentul R1 R2 (Nicovala)
    \draw[dashed, gray] (-2,0) -- (3,0);
    \draw[thick] (0,0) node[below] {$R_1$} -- (2,0) node[below] {$R_2$};
    
    % Desenam micile delimitari laterale
    \draw[dashed, gray] (0,-0.5) -- (0,1.5);
    \draw[dashed, gray] (2,-0.5) -- (2,1.5);

    % Vectorii normala (n) si tangenta (t)
    \draw[->, thick, blue] (0,0) -- (0,1) node[right] {$\vec{n}$};
    \draw[->, thick, red] (0,0) -- (1,0) node[below] {$\vec{t}$};

    % Segmentul incident (Ciocanul) care traverseaza planul
    \draw[thick] (-1, 1.5) -- (1.5, -0.5);
    \filldraw (-1, 1.5) circle (1pt) node[left] {$J_1$};
    \filldraw (1.5, -0.5) circle (1pt) node[right] {$J_2$};

    % Punctul de intersectie calculat prin interpolare
    \filldraw (0.875, 0) circle (1.5pt) node[above right] {$J'_1$};
\end{tikzpicture}
\caption{Reprezentarea tăierii laturii incidente în raport cu planul de referință.}
\end{figure}

După obținerea normalei de coliziune și a adâncimii de penetrare cu ajutorul algoritmului SAT , este necesară găsirea punctelor exacte de contact (manifold-ul) pentru a ști unde să aplicăm forțele de răspuns. Vom folosi un algoritm bazat pe tăierea poligoanelor (clipping), similar cu algoritmul Sutherland-Hodgman.

Pentru a rezolva coliziunea, trebuie să aflăm originea acestui vector pentru a putea stabili cum trebuie mutate corpurile la ciocnire sau rezemare.

Primul pas este identificarea părților din corpuri care se ating efectiv. Presupunem că un corp $A$ (incident) intră într-un corp $B$ (referință). Pentru a simplifica problema și a oferi generalitate, corpul $B$ se va comporta ca o suprafață plană (o "nicovală"), minimizând problema la a găsi latura din $B$ în care corpul $A$ se înfige.

Știind direcția din care vine impactul, calculăm produsele scalare $\langle \vec{n}, \vec{n}_{latura} \rangle$ între normala de coliziune obținută din SAT și normalele locale ale fiecărei laturi din corpul $B$, pentru a afla ce axă este mai apropiată de cea pe care trebuie să ne mișcăm. 

Valoarea maximă a acestui produs scalar ne indică latura lovită. Odată identificată această latură de referință pe corpul $B$, definim sistemul de coordonate al contactului:
\begin{itemize}
    \item Normala suprafeței de contact, $\vec{n}_{ref}$, ca fiind normala acestei laturi.
    \item Vectorul tangent, $\vec{t}$, obținut prin rotirea normalei cu $90^\circ$ în plan. Acesta este tangent la suprafața de contact și indică direcția de-a lungul laturii.
\end{itemize}

În mod analog, analizăm laturile corpului $A$ în raport cu direcția opusă ($-\vec{n}$) pentru a găsi \textit{latura incidentă} (cea care se ciocnește de planul lui $B$). Prin reducerea intersecției volumelor la aceste două segmente esențiale, putem aplica un algoritm de decupare (clipping) pentru a extrage exact punctele finale de contact.

\subsubsection{Extragerea punctelor de contact (Clipping)}

Se identifică latura de referință (nicovala) pe corpul lovit, definită de punctele $R_1$ și $R_2$, și latura incidentă (ciocanul) pe corpul care lovește, definită de punctele $J_1$ și $J_2$.

Versorul tangent devine:
$$ \vec{t} = \frac{\vec{R}_2 - \vec{R}_1}{||\vec{R}_2 - \vec{R}_1||} $$
Iar normala la suprafața de contact se definește ca:
$$ \vec{n} = (-t_y, t_x) $$

Algoritmul presupune trecerea segmentului incident $J_1 J_2$ prin trei plane de tăiere.

\begin{figure}[h!]
\centering
\begin{tikzpicture}[scale=1.15, >=stealth]

    % Setari generale pentru desen
    \tikzstyle{nicovala} = [thick]
    \tikzstyle{hasura} = [pattern=north east lines, pattern color=gray!80]
    \tikzstyle{plan} = [dashed, gray, thick]
    \tikzstyle{segment} = [thick, blue]
    \tikzstyle{punct} = [circle, fill, inner sep=1.2pt]
    \tikzstyle{punct_taiat} = [circle, fill=red, inner sep=1.2pt]

    % ================= a) Taierea la stanga =================
    \begin{scope}[shift={(0,0)}]
        % Hasura teren
        \fill[hasura] (-0.5,0) rectangle (2.5,-0.2);
        \draw[nicovala] (-0.5,0) -- (2.5,0);
        
        \node[below right] at (0,0) {$R_1$};
        \node[below left] at (2,0) {$R_2$};

        % Planul 1 de taiere
        \draw[plan] (0,-0.5) -- (0,1.5) node[above, text=black] {Plan 1};

        % Segmentul incident initial
        \draw[segment] (-0.8, 1.2) -- (2.5, -0.6);
        \node[left, text=black] at (-0.8, 1.2) {$J_1$};
        \node[right, text=black] at (2.5, -0.6) {$J_2$};
        
        \node[punct, blue] at (-0.8, 1.2) {};
        \node[punct, blue] at (2.5, -0.6) {};

        \draw[->, very thick] (0.6, 0) -- (0.6, 0.6) node[above] {$\vec{\boldsymbol{n}}$};
        \draw[->, very thick] (0.6, 0) -- (1.2, 0) node[below] {$\vec{\boldsymbol{\tau}}$};

        % Punctul nou calculat J'_1
        \node[punct_taiat] at (0, 0.76) {};
        \node[above right, red] at (0, 0.76) {$J'_1$};
        
        \node[below] at (1,-1) {\textbf{a) Tăiere la $R_1$}};
    \end{scope}

    % ================= b) Taierea la dreapta =================
    \begin{scope}[shift={(4.8,0)}]
        % Hasura teren
        \fill[hasura] (-0.5,0) rectangle (2.5,-0.2);
        \draw[nicovala] (-0.5,0) -- (2.5,0);
        
        \node[below right] at (0,0) {$R_1$};
        \node[below left] at (2,0) {$R_2$};

        % Planul 2 de taiere
        \draw[plan] (2,-0.5) -- (2,1.5) node[above, text=black] {Plan 2};

        % Segmentul incident curent
        \draw[segment] (0, 0.76) -- (2.5, -0.6);
        \node[above left, text=black] at (0, 0.76) {$J'_1$};
        \node[right, text=black] at (2.5, -0.6) {$J_2$};
        
        \node[punct, blue] at (0, 0.76) {};
        \node[punct, blue] at (2.5, -0.6) {};


        \draw[->, very thick] (0.6, 0) -- (0.6, 0.6) node[above] {$\vec{\boldsymbol{n}}$};
        \draw[->, very thick] (0.6, 0) -- (1.2, 0) node[below] {$\vec{\boldsymbol{\tau}}$};

        % Punctul nou calculat J'_2
        \node[punct_taiat] at (2, -0.33) {};
        \node[above right, red] at (2, -0.33) {$J'_2$};

        \node[below] at (1,-1) {\textbf{b) Tăiere la $R_2$}};
    \end{scope}

    % ================= c) Taierea de adancime =================
    \begin{scope}[shift={(9.6,0)}]
        % Hasura teren
        \fill[hasura] (-0.5,0) rectangle (2.5,-0.2);
        \draw[nicovala] (-0.5,0) -- (2.5,0);
        
        \node[below right] at (0,0) {$R_1$};
        \node[below left] at (2,0) {$R_2$};

        % Segmentul incident "in aer" (Dashed)
        \draw[dashed, blue] (0, 0.76) -- (1.4, 0);
        \node[above left, text=black] at (0, 0.76) {$J'_1$};
        \node[punct, blue] at (0, 0.76) {};
        
        % Segmentul care a patruns in corp (Solid Red)
        \draw[very thick, red] (1.4, 0) -- (2, -0.33);
        \node[punct, blue] at (2, -0.33) {};
        \node[below right, text=black] at (2, -0.33) {$J'_2$};

        \draw[->, very thick] (0.6, 0) -- (0.6, 0.6) node[above] {$\vec{\boldsymbol{n}}$};
        \draw[->, very thick] (0.6, 0) -- (1.2, 0) node[below] {$\vec{\boldsymbol{\tau}}$};

        % Punctul de contact final la suprafata J''_1
        \node[punct_taiat] at (1.4, 0) {};
        % yshift=2pt il muta o idee mai sus ca sa nu se lipeasca de linia neagra
        \node[above right, red, yshift=2pt] at (1.4, 0) {$J''_1$}; 

        \node[below] at (1,-1) {\textbf{c) Punctul de contact}};
    \end{scope}

\end{tikzpicture}
\caption{Fazele algoritmului de clipping pentru găsirea manifold-ului de contact. Tăieturile succesive elimină punctele care nu pătrund în corpul de referință.}
\label{fig:clipping_steps}
\end{figure}

\begin{enumerate}
    \item \textbf{Tăierea la stânga lui $R_1$:} Eliminăm punctele $P$ de pe perimetrul lui $A$ pentru care distanța $d(P) = \langle (P - R_1), \vec{t} \rangle < 0$.
    \item \textbf{Tăierea la dreapta lui $R_2$:} Eliminăm punctele pentru care distanța $d(P) = \langle (P - R_2), -\vec{t} \rangle < 0$.
    \item \textbf{Tăierea de adâncime (deasupra lui $R_1 R_2$):} Eliminăm punctele aflate deasupra suprafeței de contact, adică $d(P) = \langle (P - R_1), \vec{n} \rangle > 0$. Salvăm doar punctele cu $d(P) \le 0$, acestea reprezentând zona de intersecție reală.
\end{enumerate}

Pentru a găsi punctul exact de intersecție $J'$ cu un plan, se verifică produsul distanțelor. Deoarece nu putem verifica o infinitate de puncte, ne bazăm pe capetele segmentului. Dacă $d(J_1) \cdot d(J_2) < 0$, capetele se află de o parte și de alta a planului, indicând o traversare a frontierei. În acest caz, se folosește interpolarea liniară.

Factorul de interpolare $t$ se calculează astfel:
$$ t = \left| \frac{d(J_1)}{d(J_1) - d(J_2)} \right| $$
Noul punct valid va înlocui punctul din exterior, având coordonatele calculate pe componente:
$$ J'_1 = J_1 + t(J_2 - J_1) $$

După aplicarea acestor trei tăieturi succesive, obținem 1 sau 2 puncte de contact finale ($J_1$ și $J_2$ trunchiate). În aceste puncte se va aplica forța de impact.
\subsection{Rezolvarea coliziunilor: Corecția de poziție și calculul impulsului}

\subsubsection{Corecția de poziție (Positional Projection)}
În realitate, corpurile solide nu se interpătrund. În program, acest lucru se întâmplă din cauza cuantelor discrete de timp $\Delta t$. Înainte de a aplica impulsurile pe corpuri pentru a modifica vitezele, trebuie să corectăm acest fenomen.

Cunoscând adâncimea de coliziune $l$ și normala la intersecție $\vec{n}$ din algoritmul SAT, corecția o vom face prin mutarea relativă a corpurilor. Pentru a asigura stabilitatea obiectelor de tip perete (cu masă infinită), mutarea se ponderează folosind \textit{masa inversă} a corpurilor. 

Notăm masele inverse cu $invM_A = \frac{1}{m_A}$ și $invM_B = \frac{1}{m_B}$. 
Corecția pozițiilor centrelor de masă devine:
$$ \vec{x}_{CA} = \vec{x}_{CA_{vechi}} - \vec{n} \cdot l \cdot \frac{invM_A}{invM_A + invM_B} $$
$$ \vec{x}_{CB} = \vec{x}_{CB_{vechi}} + \vec{n} \cdot l \cdot \frac{invM_B}{invM_A + invM_B} $$

Acest calcul asigură faptul că mișcarea este direct proporțională cu masa inversă (obiectele grele sunt mutate mai puțin). O masă infinită ($invM = 0$) va prelua corecție $0\%$.

\subsubsection{Demonstrația analitică a impulsului de coliziune}

\begin{figure}[h!]
\centering
\begin{tikzpicture}[scale=1.5, >=stealth]

    % 1. Axele globale (Sistemul fix OXY)
    \draw[->, thick] (0,0) -- (8.0,0) node[below] {$X$};
    \draw[->, thick] (0,0) -- (0,5.5) node[left] {$Y$};
    \node[below left] at (0,0) {$O$};

    % 2. Corpul rigid 'A'
    \draw[thick, fill=blue!5] plot [smooth cycle, tension=0.7] coordinates {
        (1.5, 2.0) (3.2, 1.8) (4.3, 2.9) (3.5, 4.5) (1.5, 4.0)
    };
    \node[blue!80!black, font=\large] at (1.8, 3.8) {$A$};

    \coordinate (CA) at (2.6, 2.9);
    % Mutam textul in stanga punctului
    \filldraw (CA) circle (1.5pt) node[left=4pt] {$C_A$};

    % 3. Corpul rigid 'B'
    \draw[thick, fill=red!5] plot [smooth cycle, tension=0.7] coordinates {
        (4.3, 2.9) (5.5, 1.8) (7.2, 2.5) (7.0, 4.5) (5.2, 4.6)
    };
    \node[red!80!black, font=\large] at (6.8, 4.0) {$B$};

    \coordinate (CB) at (5.8, 3.0);
    % Mutam textul in dreapta punctului
    \filldraw (CB) circle (1.5pt) node[below=4pt] {$C_B$};

    % 4. Punctul de contact C
    \coordinate (C) at (4.3, 2.85); 
    \filldraw (C) circle (1.5pt) node[below=5pt] {$C$};

    % 5. Axele locale si unghiurile
    % Pentru corpul A
    \draw[->, thick, gray!80!black] (CA) -- ++(25:0.9) node[right=-1pt] {$\bar{i}_A$};
    \draw[->, thick, gray!80!black] (CA) -- ++(115:0.9) node[above] {$\bar{j}_A$};
    \draw[->, gray] (CA) ++(0.5,0) arc (0:25:0.5) node[midway, right=1pt, yshift=2pt] {$\varphi_A$};
    \draw[dashed, gray] (CA) -- ++(0.6,0);

    % Pentru corpul B
    \draw[->, thick, gray!80!black] (CB) -- ++(-15:0.9) node[right] {$\bar{i}_B$};
    \draw[->, thick, gray!80!black] (CB) -- ++(75:0.9) node[above] {$\bar{j}_B$};
    \draw[->, gray] (CB) ++(0.5,0) arc (0:-15:0.5) node[midway, right=1pt, yshift=-2pt] {$\varphi_B$};
    \draw[dashed, gray] (CB) -- ++(0.6,0);

    % 6. Vectorii globali de pozitie (mutam textul mai jos pe sageata - pos=0.5)
    \draw[->, very thick, green!60!black] (0,0) -- (CA) node[pos=0.5, left=2pt] {$\bar{r}_A$};
    \draw[->, very thick, green!60!black] (0,0) -- (CB) node[pos=0.5, below right=1pt] {$\bar{r}_B$};

    % 7. Vectorii relativi de la centre la punctul de contact (text deasupra liniilor)
    \draw[->, very thick, purple] (CA) -- (C) node[pos=0.85, above=1.2pt] {$\bar{r}_{Ac}$};
    \draw[->, very thick, purple] (CB) -- (C) node[pos=0.5, above=2pt] {$\bar{r}_{Bc}$};

    % 8. Normala si Percutiile de Impact
    \def\ang{-15} 
    
    % Normala de contact n (albastru)
    \draw[->, very thick, blue] (C) -- ++(\ang:1.1) node[below right] {$\bar{n}$};
    
    % Percutia pe B (A actiuneaza asupra lui B) -> sageata rosie mutata mai SUS
    \draw[->, very thick, red!90!black] ([yshift=2.5mm]C) -- ++(\ang:1.0) node[above left=14pt] {$P\bar{n}$};
    
    % Percutia pe A (Reactiunea dinspre B) -> sageata rosie mutata mai JOS ca sa evite CA
    \draw[->, very thick, red!90!black] ([yshift=-2.5mm]C) -- ++(\ang+180:1.0) node[below left=2pt] {$-P\bar{n}$};

\end{tikzpicture}
\caption{Reprezentarea coliziunii între două corpuri rigide $A$ și $B$. Punctul de contact $C$ este definit prin vectorii de poziție relativi $\bar{r}_{Ac}$ și $\bar{r}_{Bc}$. Interacțiunea generează percuțiile $+P\bar{n}$ și $-P\bar{n}$ care acționează de-a lungul normalei de coliziune $\bar{n}$.}
\label{fig:ciocnire_corpuri}
\end{figure}


Considerăm două corpuri care se ciocnesc, având masele $m_A, m_B$ și momentele de inerție $J_A, J_B$. Fie punctul de contact $C$. Vectorii $\bar{r}_{Ac}$ și $\bar{r}_{Bc}$ sunt vectorii de poziție ai punctului $C$ în coordonatele celor 2 corpuri. Cunoaștem vitezele inițiale ale celor 2 corpuri: $\dot{x}_A, \dot{y}_A, \dot{x}_B, \dot{y}_B$.

Fie punctul $C_1$ (respectiv $C_2$) punctul din cele două sisteme de referință ale corpurilor. Deoarece mișcarea plan-paralelă se comportă ca o mișcare de rotație blocată în timp, introducem punctele fictive $C_1, C_2$, care sunt de fapt punctul de contact $C$ în sistemele de referință ale celor 2 corpuri. Viteza punctului $C$ se calculează astfel:
\begin{align}
    \bar{v}_{cA} &= \bar{v}_A + \bar{\omega}_A \times \bar{r}_{A} \\
    \bar{v}_{cB} &= \bar{v}_B + \bar{\omega}_B \times \bar{r}_{B} 
\end{align}

În timpul ciocnirii, apare o percuție $P_A$ orientată pe direcția normală la ciocnire. Din principiul acțiunii și reacțiunii, primului corp i se aplică percuția $P_A$, iar celuilalt corp percuția $P_B$:
\begin{equation}
    \bar{P}_A = P \cdot \bar{n} \quad ; \quad \bar{P}_2 = -P \cdot \bar{n} 
\end{equation}

Aplicând teorema impulsului ($\bar{H}'' - \bar{H}' = \sum \bar{P}$)  și proiectând pe axele sistemului global, obținem ecuațiile pentru ambele corpuri:
\begin{align}
    ox: m_A \dot{x}_A'' - m_A \dot{x}_A' &= \langle P \cdot \bar{n}, \bar{i} \rangle = P \cdot n_x \\
    oy: m_A \dot{y}_A'' - m_A \dot{y}_A' &= \langle P \cdot \bar{n}, \bar{j} \rangle = P \cdot n_y \\
    ox: m_2 \dot{x}_B'' - m_2 \dot{x}_B' &= \langle -P \cdot \bar{n}, \bar{i} \rangle = -P \cdot n_x \\
    oy: m_2 \dot{y}_B'' - m_2 \dot{y}_B' &= \langle -P \cdot \bar{n}, \bar{j} \rangle = -P \cdot n_y \\
\end{align}

\paragraph{Calculul momentului cinetic}
Momentul cinetic se definește ca $\bar{K}_0 = \bar{r} \times m\bar{v} + \bar{J}\bar{\omega}$. Deoarece vectorii se află în plan, rezultatul produsului vectorial are doar componentă pe axa Z, deci il putem considera ca un scalar pentru usurinta notatiilor.
\begin{equation}
    K_0 = m x \dot{y} - m y \dot{x} + J \dot{\varphi} 
\end{equation}

În formă vectorială, aplicăm teorema momentului cinetic:
\begin{equation}
    \bar{K}_0'' - \bar{K}_0' = \sum \bar{r}_i \times \bar{P}_i \implies \bar{K}_0'' - \bar{K}_0' = \bar{r}_{1c} \times \bar{P} 
\end{equation}

Calculăm produsul vectorial cu ajutorul determinantului:
\begin{equation}
    \begin{vmatrix} 
    \bar{i} & \bar{j} & \bar{k} \\ 
    x_{Ac} & y_{Ac} & 0 \\ 
    P_x & P_y & 0 
    \end{vmatrix} = \bar{k}(x_{Ac} P_y - y_{Ac} P_x) 
\end{equation}

Aplicăm aceasta pentru corpul A:
\begin{equation}
    m_A x_A'' \dot{y}_A'' - m_A y_A'' \dot{x}_A'' + J_A \dot{\varphi}_A'' - (m_A x_A' \dot{y}_A' - m_A y_A' \dot{x}_A' + J_A \dot{\varphi}_A') = x_{Ac} P_y - y_{Ac} P_x 
\end{equation}

Pentru că ciocnirea are loc într-un timp foarte scurt, pozițiile rămân neschimbate: $x''=x'$ și $y''=y'$. Substituim relațiile pentru vitezele liniare $\dot{x}_A''$ și $\dot{y}_A''$:
\begin{equation}
    m_A x_A \frac{P n_y + m_A \dot{y}_A'}{m_A} - m_A y_A \frac{P n_x + m_A \dot{x}_A'}{m_A} + J_A \dot{\varphi}_A'' - m_A x_A' \dot{y}_A' + m_A y_A' \dot{x}_A' - J_A \dot{\varphi}_A' = x_{Ac} P n_y - y_{Ac} P n_x 
\end{equation}

Desfacem parantezele și simplificăm masele de la numărător și numitor:
\begin{equation}
    x_A P n_y + x_A m_A \dot{y}_A' - y_A P n_x - y_A m_A \dot{x}_A' + J_A \dot{\varphi}_A'' - m_A x_A \dot{y}_A' + m_A y_A \dot{x}_A' - J_A \dot{\varphi}_A' = x_{Ac} P n_y - y_{Ac} P n_x
\end{equation}

Termenii opuși se anulează (ex: $x_A m_A \dot{y}_A'$ se reduce cu $- m_A x_A \dot{y}_A'$). Izolăm $J_A \dot{\varphi}_A''$:
\begin{equation}
    J_A \dot{\varphi}_A'' = x_{Ac} P n_y - y_{Ac} P n_x - x_A P n_y + y_A P n_x + J_A \dot{\varphi}_1'
\end{equation}

Dând factor comun componentele normalei și împărțind la inerție, obținem expresia finală pentru viteza unghiulară:
\begin{equation}
    \dot{\varphi}_A'' = \frac{P}{J_A} \left[ n_x(y_A - y_{Ac}) + n_y(-x_A + x_{Ac}) \right] + \dot{\varphi}_A' 
\end{equation}

Putem generaliza formulele finale de actualizare a vitezelor pentru oricare dintre cele două corpuri (ținând cont de semnul percuției):
\begin{align}
    \dot{x}_{A,B}'' &= \frac{P n_x + m_{A,B} \dot{x}_{A,B}'}{m_{A,B}} \\
    \dot{y}_{A,B}'' &= \frac{P n_y + m_{A,B} \dot{y}_{A,B}'}{m_{A,B}} \\
    \dot{\varphi}_{A,B}'' &= \frac{P}{J_{A,B}} \left[ n_x(y_{A,B} - y_{A,Bc}) + n_y(-x_{A,B} + x_{A,Bc}) \right] + \dot{\varphi}_{A,B}'
\end{align}

\subsection{Calculul percuției și vitezelor post-impact}

Cunoaștem coeficientul de restituire $k$ (sau $e$) dintre cele două corpuri. Conform legii lui Newton pentru ciocniri, diferența vitezelor pe direcția normalei la suprafața de contact, după impact, este proporțională și de sens opus cu diferența vitezelor de dinainte de impact:
\begin{equation}
    v_{CB,n}'' - v_{CA,n}'' = -k \left( v_{CB,n}' - v_{CA,n}' \right)
\end{equation}
unde $v_{C,n} = \bar{v}_C \cdot \bar{n}$ reprezintă proiecția vitezei punctului de contact pe versorul normal $\bar{n}$.

Vitezele punctelor de contact considerate ca aparținând corpului $A$, respectiv corpului $B$, se scriu folosind relația de distribuție a vitezelor:
\begin{equation}
    \begin{aligned}
        \bar{v}_{CA}'' &= \bar{v}_A'' + \bar{\omega}_A'' \times \bar{r}_{A} = \begin{bmatrix} \dot{x}_A'' \\ \dot{y}_A'' \end{bmatrix} + \begin{vmatrix} \bar{i} & \bar{j} & \bar{k} \\ 0 & 0 & \dot{\varphi}_A'' \\ x_{A} & y_{A} & 0 \end{vmatrix} = \begin{bmatrix} \dot{x}_A'' - y_{A} \dot{\varphi}_A'' \\ \dot{y}_A'' + x_{A} \dot{\varphi}_A'' \end{bmatrix} \\
        \bar{v}_{CB}'' &= \bar{v}_B'' + \bar{\omega}_B'' \times \bar{r}_{B} = \begin{bmatrix} \dot{x}_B'' \\ \dot{y}_B'' \end{bmatrix} + \begin{vmatrix} \bar{i} & \bar{j} & \bar{k} \\ 0 & 0 & \dot{\varphi}_B'' \\ x_{B} & y_{B} & 0 \end{vmatrix} = \begin{bmatrix} \dot{x}_B'' - y_{B} \dot{\varphi}_B'' \\ \dot{y}_B'' + x_{B} \dot{\varphi}_B'' \end{bmatrix}
    \end{aligned}
\end{equation}
\textit{(Notă: am notat cu $x_{A}, y_{A}$ și $x_{B}, y_{B}$ coordonatele relative ale punctelor de contact față de centrele de masă ale celor două corpuri).}

Efectuăm produsul scalar al fiecărei viteze cu versorul normal $\bar{n} = [n_x, n_y]^T$ pentru a obține proiecțiile individuale pe direcția ciocnirii:
\begin{equation}
    \begin{aligned}
        v_{CA,n}'' &= \bar{v}_{CA}'' \cdot \bar{n} = (\dot{x}_A'' - y_A \dot{\varphi}_A'')n_x + (\dot{y}_A'' + x_A \dot{\varphi}_A'')n_y \\
                   &= \dot{x}_A'' n_x + \dot{y}_A'' n_y + \dot{\varphi}_A''(x_A n_y - y_A n_x) \\
        v_{CB,n}'' &= \bar{v}_{CB}'' \cdot \bar{n} = (\dot{x}_B'' - y_B \dot{\varphi}_B'')n_x + (\dot{y}_B'' + x_B \dot{\varphi}_B'')n_y \\
                   &= \dot{x}_B'' n_x + \dot{y}_B'' n_y + \dot{\varphi}_B''(x_B n_y - y_B n_x)
    \end{aligned}
\end{equation}

Înlocuim vitezele modificate de percuția $P$. Considerând că normala $\bar{n}$ este orientată de la A la B (deci corpul B primește percuția $+P$, iar corpul A primește reacțiunea $-P$):
\begin{equation}
    \begin{cases}
        \dot{x}_B'' = \dot{x}_B' + \frac{P n_x}{m_B} \\
        \dot{y}_B'' = \dot{y}_B' + \frac{P n_y}{m_B} \\
        \dot{\varphi}_B'' = \dot{\varphi}_B' + \frac{P}{J_B}(x_B n_y - y_B n_x)
    \end{cases}
    \qquad \text{și} \qquad
    \begin{cases}
        \dot{x}_A'' = \dot{x}_A' - \frac{P n_x}{m_A} \\
        \dot{y}_A'' = \dot{y}_A' - \frac{P n_y}{m_A} \\
        \dot{\varphi}_A'' = \dot{\varphi}_A' - \frac{P}{J_A}(x_A n_y - y_A n_x)
    \end{cases}
\end{equation}

Introducem aceste expresii în ecuațiile proiecțiilor pe normală și, ținând cont că $n_x^2 + n_y^2 = 1$, componentele se grupează astfel:
\begin{equation}
    \begin{aligned}
        v_{CA,n}'' &= \left[ \dot{x}_A' n_x + \dot{y}_A' n_y + \dot{\varphi}_A'(x_A n_y - y_A n_x) \right] - P \left[ \frac{1}{m_A} + \frac{(x_A n_y - y_A n_x)^2}{J_A} \right] \\
                   &= v_{CA,n}' - P \left[ \frac{1}{m_A} + \frac{(x_A n_y - y_A n_x)^2}{J_A} \right] \\
        v_{CB,n}'' &= \left[ \dot{x}_B' n_x + \dot{y}_B' n_y + \dot{\varphi}_B'(x_B n_y - y_B n_x) \right] + P \left[ \frac{1}{m_B} + \frac{(x_B n_y - y_B n_x)^2}{J_B} \right] \\
                   &= v_{CB,n}' + P \left[ \frac{1}{m_B} + \frac{(x_B n_y - y_B n_x)^2}{J_B} \right]
    \end{aligned}
\end{equation}

Scădem cele două relații pentru a forma diferența vitezelor pe normală după impact:
\begin{equation}
    v_{CB,n}'' - v_{CA,n}'' = (v_{CB,n}' - v_{CA,n}') + P \left[ \frac{1}{m_A} + \frac{1}{m_B} + \frac{(x_A n_y - y_A n_x)^2}{J_A} + \frac{(x_B n_y - y_B n_x)^2}{J_B} \right]
\end{equation}

Știind din legea ciocnirilor că $v_{CB,n}'' - v_{CA,n}'' = -k (v_{CB,n}' - v_{CA,n}')$, obținem în final formula percuției $P$:
\begin{equation}
    P = \frac{-(1 + k) (v_{CB,n}' - v_{CA,n}')}{\frac{1}{m_A} + \frac{1}{m_B} + \frac{(x_A n_y - y_A n_x)^2}{J_A} + \frac{(x_B n_y - y_B n_x)^2}{J_B}}
\end{equation}

Odată calculat scalarul $P$, vitezele finale de translație și rotație ale ambelor corpuri se obțin prin simpla sa înlocuire în sistemele de ecuații cinematice anterioare.
\section{Implementare si Arhitectura Software}
\subsection{Structura aplicatiei: C++, organizarea pe componente si OpenGL pentru randare grafica.}
\subsection{Rezolvarea sistemului de constrângeri pe accelerații.}
\subsection{Integrarea numerica.}

\section{Rezultate si Validarea Simularii}
\subsection{Conservarea energiei: Analiza termodinamica a sistemului in miscare.}
\subsection{Teste de stabilitate: Prezentarea simularilor (lant articulat, pendul multiplu, stiva de corpuri).}
\subsection{Analiza performantei: Impactul filtrului AABB asupra timpului de calcul pe cadru.}

\section{Concluzii si Dezvoltari Ulterioare}
\subsection{Obiective atinse si limitari curente.}
\subsection{ Directii viitoare de dezvoltare.}

\section*{Bibliografie}

\begin{thebibliography}{ }
\bibitem{text1} Real-Time Collision Detection -- Christer Ericson
\bibitem{text2} Physically Based Modelling -- Andrew Witkin
\bibitem{text3} Mecanica -- Andrei Craifaleanu
\bibitem{text4} \url{ https://www.geeksforgeeks.org/maths/cholesky-factorization/ }
\bibitem{text5} \url{https://math.stackexchange.com/questions/2106402/proof-of-separating-axis-theorem-for-polygons}
\end{thebibliography}

\end{document}
